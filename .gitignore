# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# Environments
.env

# Large generated sample set (regenerate locally via 01_generate_samples.ipynb)
data/generated/naloxone_kit_distributions.csv

# Large real training data, and trained model artifacts (not cleared for public release)
data/generated/simulation_training_data.csv
models/

# Verified against the public pph-collective/profound-model repo (byte-identical
# hash match): calibration/, ProgramData.xlsx, and InitialPopulation.rds are
# already public and included below. MasterTable.xlsx is NOT included there —
# the public version only has data through 2019, this one goes through 2023
# with updated values — so it stays excluded, along with files that don't
# exist in the public repo at all.
R_Files/Inputs/MasterTable.xlsx
R_Files/Inputs/init_pop.rds
R_Files/ODdeaths.xlsx
R_Files/.Rhistory
R_Files/.Rproj.user/
